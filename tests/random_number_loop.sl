def val WitajSwiecie = Hello, World!

"every 2000 {"
"def mut val randint = $Randint(1, 10)"
"$Println($randint)"
"}"

every 2000 {
    def mut val randint = $Randint(1, 10)
    $Println(Twoj losowy numer: $randint)
}